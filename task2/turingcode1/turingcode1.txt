编号 19

#include<stdio.h>
#include<malloc.h>
#define NULL 0
#define LEN sizeof(struct ListNode)
struct ListNode
{
	int val;
	struct ListNode *next;
};
int n;

struct ListNode *creat(void)
{
	struct ListNode *head;
	struct ListNode *p1, *p2;
	n = 0;
	p1 = p2 = (struct ListNode*)malloc(LEN);
	scanf_s("%d", &p1->val);
	head = NULL;
	while (p1->val != 0)
	{
		n = n + 1;
		if (n == 1) head = p1;
		else p2->next = p1;
		p2 = p1;
		p1 = (struct ListNode*)malloc(LEN);
		scanf_s("%d", &p1->val);
	}
	p2->next = NULL;
	return(head);
}

void main()
{
	struct ListNode*l1, *l2;
	l1= creat();
	l2= creat();
	int a[10],b[10],i,j,k,ii,jinw=0;
	
	if(l1->next!=NULL)
	do
	{
	i=0;
	a[i]=l1->val;
	i++;
	l1->next;
	}while(l1->next!=NULL);
	
	if(l2->next!=NULL)
	do
	{
	j=0;
	b[j]=l2->val;
	j++;
	l2->next;
	}while(l2->next!=NULL);
	
	if(i<=j) 	{ for(k=0;k<i;k++)
	         {
	          b[k]=a[k]+b[k]+jinw;
           	jinw=b[k]/10;
	          b[k]=b[k]%10;
	       }
	          b[k]=b[k]+jinw;
	      for(ii=0;ii<j;ii++)
	      printf("%d",b[ii]);
	           }
else { for(k=0;k<j;k++)
{a[k]=a[k]+b[k]+jinw;
jinw=a[k]/10;
a[k]=a[k]%10;
}
a[k]=a[k]+jinw;
for(ii=0;ii<i;ii++)
printf("%d",a[ii]);
}
